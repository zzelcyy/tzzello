import React, { FC } from 'react';
import styled from 'styled-components';

export const Header: FC = () => <HeaderContainer>tzzello</HeaderContainer>;

const HeaderContainer = styled.header`
  text-align: center;
  height: 24px;
  padding: 10px 16px;
`;
